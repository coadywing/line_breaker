# Claude Code Library

Reusable Claude Code assets for this template live here.

- `agents/` contains specialized reviewer agents.
- `rules/` contains workflow and coding-convention guidance.
- `skills/` contains slash-command definitions.
- `settings/` contains template Claude settings files.

Copy the pieces you want into `.claude/` when initializing a new project.
