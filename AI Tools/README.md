# AI Tools

This directory stores reusable AI-tooling assets for new projects.

- `claude code/` contains the current Claude Code library: agents, rules, skills, and template settings.
- `codex/` is the planned home for Codex-specific instructions, workflows, and helper assets.

The intent is to keep tool libraries versioned here while leaving runtime folders such as `.claude/` empty until a project opts in.
